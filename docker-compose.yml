services:
  qtris-server:
    build: ./server
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    restart: unless-stopped
  qtris-client:
    build: ./client
    ports:
      - "5173:80"
    depends_on:
      - qtris-server
    restart: unless-stopped
